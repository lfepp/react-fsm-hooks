// import * as React from 'react';
// import { mount } from 'enzyme';

import useFSM from '.';

describe('useFSM()', () => {
  it('is just a test for the initial commit', () => {
    const test = useFSM();
    expect(test).toBe(1);
  });

  // it('should require initial state arg', () => {});

  // it('should require states arg');

  // it('should require transitions arg');
});
